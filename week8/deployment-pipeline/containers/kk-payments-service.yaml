apiVersion: v1
kind: Service
metadata:
  name: kk-payments
spec:
  type: NodePort
  selector:
    app: kk-payments
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
